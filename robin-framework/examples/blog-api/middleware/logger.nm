// Request logger middleware
export function logger(req, res, next) {
  const method = req.method;
  const path = req.path;
  const timestamp = "2024-01-01 10:00:00"; // Simplified timestamp
  
  println("[" + timestamp + "] " + method + " " + path);
  
  next();
}
